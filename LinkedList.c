//Singly linked list in C
#include <stdio.h>;
#include <stdlib.h>;

//Node creation
struct DataNode
{
    /* data */
    char Data[50];
    struct DataNode *next;
};
struct DataNode *head, *ptr;

ptr = (struct DataNode*)malloc(sizeof(struct DataNode*));

void main(){
    printf("************MAIN MENU**********\n");
    printf("Choose an option\n");
    printf("")
}